# Deploy static shell only. AI must stay on each user's machine (Local-First).
cd ~/surf.ai && git pull origin main
cd ~/surf.ai/apps/web && npm ci
# Do NOT set NEXT_PUBLIC_HOST_URL=https://synap.surf — that used to send chat to the VPS.
# Chat/Moss/Ollama always use http://127.0.0.1:18765 (Small Cloud on the user's PC).
npm run build
sudo rm -rf /var/www/synap.surf/* && sudo cp -a out/. /var/www/synap.surf/
# Optional: keep synap-host for auth/feedback demos only — not required for local AI chat.
sudo systemctl restart synap-host
